{% extends 'base.html.twig' %}

{% block title %}Signup{% endblock %}

{% block body %}
    <h1>Signup</h1>

    {{ form_start(form) }}

        {{ form_rest(form) }}
        <button type="submit">Save</button>

    {{ form_end(form) }}


{% endblock %}
